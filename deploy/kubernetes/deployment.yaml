apiVersion: v1
kind: Namespace
metadata:
  name: ddnsbridge4extdns
---
apiVersion: v1
kind: Secret
metadata:
  name: ddns-tsig
type: Opaque
stringData:
  # IMPORTANT: Replace these values with your own TSIG key and secret
  # Generate a secret with: echo -n "your-secret" | base64
  tsig-key: "opnsense_register"
  tsig-secret: "NDybxELWemcUGCt8N0Qqnursx+iSzURDiS+S9q8YGeyuj1y/u5Ipm8GayuU0N9o2WoIQwcTmcSlXrCMI2KVsgA=="
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: ddns-config
data:
  LISTEN_ADDR: "0.0.0.0"
  PORT: "5353"
  TSIG_ALGORITHM: "hmac-sha256"
  DNS_ENDPOINT_CRD: "true"
  ALLOWED_ZONES: "example.com,example.org"
  LOG_LEVEL: "info"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ddnsbridge4extdns
spec:
  replicas: 1
  template:
    spec:
      serviceAccountName: ddnsbridge4extdns
      containers:
      - name: ddnsbridge4extdns
        image: ddnsbridge4extdns:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5353
          name: dns-udp
          protocol: UDP
        - containerPort: 5353
          name: dns-tcp
          protocol: TCP
        env:
        - name: TSIG_KEY
          valueFrom:
            secretKeyRef:
              name: ddns-tsig
              key: tsig-key
        - name: TSIG_SECRET
          valueFrom:
            secretKeyRef:
              name: ddns-tsig
              key: tsig-secret
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        envFrom:
        - configMapRef:
            name: ddns-config
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: ddnsbridge4extdns-service
  annotations:
    kube-vip.io/loadbalancerIPs: 192.168.5.22
spec:
  ports:
  - port: 53
    targetPort: 5353
    protocol: UDP
    name: dns-udp
  - port: 53
    targetPort: 5353
    protocol: TCP
    name: dns-tcp
  externalTrafficPolicy: Local
  type: LoadBalancer
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ddnsbridge4extdns
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: ddnsbridge4extdns
rules:
- apiGroups: ["externaldns.k8s.io"]
  resources: ["dnsendpoints"]
  verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: ddnsbridge4extdns
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: ddnsbridge4extdns
subjects:
- kind: ServiceAccount
  name: ddnsbridge4extdns
  namespace: ddnsbridge4extdns
